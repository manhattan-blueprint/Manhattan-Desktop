using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace View {
    // Fake class attach to each model that is placeable on the grid AND is a machine
    public class MachinePlaceable : Placeable {
    }
}
